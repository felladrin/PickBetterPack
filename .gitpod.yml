image: mcr.microsoft.com/playwright:focal

tasks:
  - name: Dev Server
    before: nvm install --lts --latest-npm
    init: npm ci
    command: npm run dev

ports:
  - name: Dev Server
    port: 3000
    onOpen: open-preview

vscode:
  extensions:
    - lokalise.i18n-ally
